schema_version: openmed.training.recipe.v1
preset_name: laptop_lora
mode: B
backbone:
  model_ref: openmed/backbones/laptop-clinical-lora-base
  revision: policy-spine-v1
  family: encoder-token-classification
dapt:
  corpus_ref: sha256:44603cb4a282d1f308063c1ebfa6651493071bcae2ae28856884533c42aebc25
  sequence_length: 1024
lora:
  target_trainable_ratio: 0.012
  rank: 16
  alpha: 32
  dropout: 0.05
  target_modules: [query, key, value, output]
label_set_ref: openmed.core.labels:CANONICAL_LABELS@policy-spine-v1
loss:
  name: focal_class_weighted
  focal_gamma: 2.0
  class_weighted: true
  class_weighting: inverse_frequency
  critical_label_weight: 3.5
  critical_labels: [PERSON, FIRST_NAME, LAST_NAME, DATE_OF_BIRTH, ID_NUM, SSN, EMAIL, PHONE, STREET_ADDRESS, ACCOUNT_NUMBER, PASSWORD, API_KEY, CREDIT_CARD, IBAN]
hard_negatives_required: true
output_tier: laptop
quantization:
  default: int8
  allow_fp32_fallback: true
seed: 3802
